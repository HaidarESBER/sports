---
phase: 10-polish-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/page.tsx, src/app/globals.css, tailwind.config.ts, src/components/ui/Button.tsx, src/components/ui/Card.tsx, src/components/ui/Input.tsx, src/components/ui/Badge.tsx, src/components/ui/LoadingSpinner.tsx, src/components/ui/Skeleton.tsx, src/components/Navbar.tsx, src/app/layout.tsx]
autonomous: false
---

<objective>
Polish UI/UX with consistent design system, create landing page, and add global navigation.

Purpose: Create a polished, professional-looking application with consistent styling, proper navigation, and an engaging landing page to attract users.
Output: Design system components, landing page, and global navbar
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/layout.tsx
@src/app/page.tsx
@src/components/ProgramCard.tsx
@tailwind.config.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Tailwind config with brand colors</name>
  <files>tailwind.config.ts, src/app/globals.css</files>
  <action>
Extend Tailwind configuration with SportPlan brand colors and design tokens.

tailwind.config.ts additions:
- Add custom color palette:
  - primary: blue shades (500: #3B82F6 as main)
  - secondary: green shades for sports/success
  - accent: orange for CTAs and highlights
- Add custom font family:
  - sans: Inter (already loaded in layout)
- Add custom border radius scale:
  - xl: 1rem, 2xl: 1.5rem
- Add custom box shadows:
  - soft: subtle elevation
  - card: card hover effect

src/app/globals.css additions:
- CSS custom properties for colors (--color-primary, etc.)
- Base styles: smooth scroll, antialiased text
- Focus ring utility classes
- Transition defaults for interactive elements
  </action>
  <verify>npm run build succeeds, new classes available</verify>
  <done>Tailwind extended with brand design tokens</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable UI components</name>
  <files>src/components/ui/Button.tsx, src/components/ui/Card.tsx, src/components/ui/Input.tsx, src/components/ui/Badge.tsx</files>
  <action>
Create a set of reusable UI components with consistent styling.

Button.tsx:
- Variants: primary, secondary, outline, ghost, danger
- Sizes: sm, md, lg
- States: loading (with spinner), disabled
- Props: variant, size, isLoading, leftIcon, rightIcon
- Proper focus rings and transitions

Card.tsx:
- Variants: default, interactive (with hover effect)
- Optional padding prop
- Consistent border radius and shadow
- Export CardHeader, CardContent, CardFooter sub-components

Input.tsx:
- Variants: text, textarea
- States: error, disabled
- Label and error message support
- Consistent border and focus styling

Badge.tsx:
- Variants: sport colors (running, swimming, cycling, strength)
- Sizes: sm, md
- Refactor sportColors/sportLabels from ProgramCard into shared constants

All components should be client components ("use client") and use forwardRef where appropriate.
  </action>
  <verify>Components compile without TypeScript errors</verify>
  <done>Reusable UI component library created</done>
</task>

<task type="auto">
  <name>Task 3: Create loading and skeleton components</name>
  <files>src/components/ui/LoadingSpinner.tsx, src/components/ui/Skeleton.tsx</files>
  <action>
Create loading state components for better UX during data fetching.

LoadingSpinner.tsx:
- Sizes: sm (16px), md (24px), lg (32px)
- Optional text prop for "Loading..." message
- Animated SVG spinner with primary color
- Centered layout option

Skeleton.tsx:
- Variants: text, circle, rectangle
- Configurable width/height
- Animated pulse effect
- Create skeleton variants:
  - SkeletonCard: matches ProgramCard/SessionCard dimensions
  - SkeletonText: single line of text
  - SkeletonAvatar: circular avatar placeholder
  </action>
  <verify>Components render with animations</verify>
  <done>Loading and skeleton components ready for use</done>
</task>

<task type="auto">
  <name>Task 4: Create global Navbar component</name>
  <files>src/components/Navbar.tsx</files>
  <action>
Create a responsive navigation bar component.

Features:
- Logo/brand link (SportPlan) to home
- Navigation links: Decouvrir, Mes Seances, Mes Programmes
- Auth state aware:
  - Logged out: show Login/Register buttons
  - Logged in: show user avatar dropdown with Profile, Settings, Logout
- Mobile responsive:
  - Hamburger menu on small screens
  - Slide-out drawer or dropdown for mobile nav
- Sticky header with subtle background blur
- Active link highlighting based on current route

Implementation:
- Client component for interactivity
- Use next/navigation for route detection
- Fetch session status from /api/auth/session or use next-auth client
- Clean transitions and hover states
  </action>
  <verify>Navbar renders correctly on different screen sizes</verify>
  <done>Responsive navbar with auth awareness created</done>
</task>

<task type="auto">
  <name>Task 5: Integrate Navbar into layout</name>
  <files>src/app/layout.tsx</files>
  <action>
Update root layout to include the Navbar globally.

Changes:
- Import Navbar component
- Add Navbar above {children}
- Add main wrapper with appropriate padding-top to account for sticky navbar
- Ensure layout works with all existing pages
- Add footer placeholder (simple copyright text)
  </action>
  <verify>All pages show navbar, no layout issues</verify>
  <done>Navbar integrated into global layout</done>
</task>

<task type="auto">
  <name>Task 6: Create landing page</name>
  <files>src/app/page.tsx</files>
  <action>
Transform the basic home page into an engaging landing page.

Sections:
1. Hero section:
   - Headline: "Planifiez, Partagez, Progressez"
   - Subheadline: Brief description of SportPlan value prop
   - CTA buttons: "Commencer" (to register), "Decouvrir" (to discover)
   - Background: subtle gradient or pattern

2. Features section (3 columns):
   - Seances personnalisees: Create custom training sessions
   - Programmes structures: Build multi-week programs
   - Suivi de progression: Track your progress over time

3. Sports section:
   - Visual grid showing supported sports with icons
   - Running, Swimming, Cycling, Strength, etc.

4. CTA section:
   - Final call to action to sign up
   - "Rejoignez la communaute SportPlan"

5. Footer:
   - Simple footer with copyright
   - Links: A propos, Contact (placeholder)

Styling:
- Use new Button, Card components
- Responsive design (mobile-first)
- Consistent spacing using Tailwind classes
- Subtle animations on scroll (optional: use CSS animations)
  </action>
  <verify>Landing page renders beautifully on all screen sizes</verify>
  <done>Professional landing page created</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Design system, Navbar, and Landing page</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000
    3. Verify landing page:
       - Hero section with CTAs
       - Features section
       - Sports grid
       - Footer
    4. Test navigation:
       - Click navbar links
       - Mobile responsive (resize browser)
       - Auth state: logged out shows Login/Register
    5. Test auth state:
       - Log in and verify navbar shows user menu
       - Profile dropdown works
    6. Check consistency:
       - Colors and typography consistent
       - Buttons and cards use new components
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] Landing page is visually appealing and responsive
- [ ] Navbar appears on all pages
- [ ] Auth state reflected correctly in navbar
- [ ] UI components (Button, Card, etc.) are reusable
- [ ] Loading states work correctly
- [ ] User approved visual verification
</verification>

<success_criteria>
- All tasks completed
- Design system established with reusable components
- Landing page professional and engaging
- Navigation consistent across all pages
- User approved the implementation
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-launch/10-01-SUMMARY.md`
</output>
