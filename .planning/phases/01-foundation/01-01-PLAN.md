---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, next.config.ts, tsconfig.json, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, tailwind.config.ts, postcss.config.mjs]
autonomous: true
---

<objective>
Initialize Next.js 14 project with TypeScript, Tailwind CSS, and standard folder structure.

Purpose: Create the foundational project structure that all subsequent phases will build upon.
Output: Running Next.js application with dev server, proper TypeScript config, and organized folder structure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with TypeScript and Tailwind</name>
  <files>package.json, next.config.ts, tsconfig.json, tailwind.config.ts, postcss.config.mjs, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm` with `--yes` flag to accept defaults.

    This creates:
    - Next.js 14 with App Router
    - TypeScript configuration
    - Tailwind CSS with PostCSS
    - ESLint for code quality
    - src/ directory structure
    - @ import alias

    Use npm (not yarn/pnpm) for consistency.
  </action>
  <verify>npm run dev starts server on localhost:3000 without errors</verify>
  <done>Next.js dev server runs successfully, TypeScript compiles, Tailwind works</done>
</task>

<task type="auto">
  <name>Task 2: Create application folder structure</name>
  <files>src/components/.gitkeep, src/lib/.gitkeep, src/types/.gitkeep, src/hooks/.gitkeep</files>
  <action>
    Create organized folder structure for the application:

    ```
    src/
    ├── app/           (already created by Next.js)
    ├── components/    (React components)
    │   └── .gitkeep
    ├── lib/           (utilities, helpers, db clients)
    │   └── .gitkeep
    ├── types/         (TypeScript type definitions)
    │   └── .gitkeep
    └── hooks/         (custom React hooks)
        └── .gitkeep
    ```

    Use .gitkeep files to preserve empty directories in git.
    Do NOT create unnecessary nested folders yet - keep it minimal.
  </action>
  <verify>ls -la src/ shows all directories exist</verify>
  <done>Folder structure created with components/, lib/, types/, hooks/ directories</done>
</task>

<task type="auto">
  <name>Task 3: Update homepage with SportPlan placeholder</name>
  <files>src/app/page.tsx</files>
  <action>
    Replace default Next.js homepage with minimal SportPlan placeholder:

    - Display "SportPlan" as h1 heading
    - Add brief tagline: "Planifiez, partagez, progressez"
    - Use Tailwind for centered layout with proper spacing
    - Keep it minimal - just enough to confirm app works

    Remove all default Next.js boilerplate content (Vercel logo, links, etc.).
  </action>
  <verify>Visit localhost:3000 shows SportPlan heading and tagline</verify>
  <done>Homepage displays SportPlan branding instead of Next.js default</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` succeeds
- [ ] TypeScript compilation has no errors
- [ ] Homepage shows SportPlan content
- [ ] All directories exist in src/
</verification>

<success_criteria>

- All tasks completed
- Next.js dev server runs on localhost:3000
- TypeScript compiles without errors
- Tailwind CSS works (visible in homepage styling)
- Folder structure established for future development
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
